# BookAppoiment - Frontend Application

A modern, responsive healthcare appointment portal built with Next.js.

## 🚀 Key Technologies
- **Next.js 15+**: React framework for production.
- **Material UI (MUI)**: For elegant UI components and layout.
- **Framer Motion**: For smooth animations and transitions.
- **Axios**: For API communication with the Django backend.
- **Lucide React**: For consistent and beautiful iconography.

## 📦 Getting Started

1. Install dependencies:
   ```bash
   npm install
   ```
2. Configure environment:
   Create a `.env.local` file to point to your backend API:
   ```text
   NEXT_PUBLIC_API_URL=http://localhost:8000
   ```
3. Run in development mode:
   ```bash
   npm run dev
   ```
4. Build for production:
   ```bash
   npm run build
   ```

## 🖌️ Design & Aesthetics
The frontend focuses on a **premium patient experience** featuring:
- Glassmorphism effects.
- Dynamic hover states.
- Responsive mobile-first layout.
- Detailed appointment tracking interfaces.

## 📝 Scripts
- `npm run dev`: Starts the Next.js development server.
- `npm run build`: Builds the application for production.
- `npm run start`: Starts the production server.
- `npm run lint`: Checks for code quality issues.
